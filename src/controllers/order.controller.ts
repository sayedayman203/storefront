import { Request, Response, NextFunction } from 'express';
export const index = (
  req: Request,
  res: Response,
  next: NextFunction
): void => {};

export const show = (
  req: Request,
  res: Response,
  next: NextFunction
): void => {};

export const create = (
  req: Request,
  res: Response,
  next: NextFunction
): void => {};
